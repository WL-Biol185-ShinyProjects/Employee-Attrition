library(readr)
attrition <- read.csv("watson_healthcare_modified.csv")
View(attrition)

regression <- function(dataframe, var1)
  
  dataframe %>%
    mutate()
    typeof(dataframe[vars]) == "character"
    
  vars <- colnames(dataframe) - dataframe[var1]
  
  
  